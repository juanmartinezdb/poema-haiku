# Instalación:
1. Preparar el Entorno Virtual
python -m venv venv
.\venv\Scripts\activate

2. Instalar Dependencias
pip install -r requirements.txt

3. renombrar el archivo .env.example a .env
cambia las xxx por tus porpias keys y buckets que vayas a usar.

# Ejecución:
python application.py

ir a la url:
http://127.0.0.1:5000

para usarlo solo hay que cargar la imagen y darle a submit